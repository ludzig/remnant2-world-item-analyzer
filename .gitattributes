# Target platform is Linux - line endings in the repo are always LF,
# so files edited on Windows don't end up as CRLF here.
* text=auto eol=lf

# Save game fixtures are binary and must never be touched.
*.sav binary
*.png binary
*.svg text eol=lf
